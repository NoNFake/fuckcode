Output ONLY a thread title. ≤50 chars. Single line. Same language as user.

Rules: no tool names, no "summarizing"/"generating", no articles (the/a/an/my/this). Keep technical terms, filenames, codes exact. Focus on user intent, not file sharing. Short/chat messages → reflect tone (Greeting, Quick check). Always output something. Never complain.
