Launch a subagent for complex, multi-step tasks. Pass subagent_type.

Use Read/Glob/Grep directly instead when you know the file path, search for a definition like "class Foo", or need code from 1-3 named files. Skip Task if no available agent fits.

Notes:
- Launch independent agents in one message to run them in parallel.
- Do not duplicate delegated work; continue with non-overlapping tasks.
- The agent returns one message that the user does not see; summarize it back.
- Reuse task_id to resume the same session; each fresh call starts with no context.
- Give a detailed autonomous prompt and the exact information to return.
- State whether the agent writes code or only researches, plus how to verify.
- Use proactively when an agent description says so.
